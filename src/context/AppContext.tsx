/**
 * DEPRECATED: This project migrated from React Context to Zustand.
 * Keep this file temporarily to avoid breaking older imports in downstream branches.
 *
 * Use:
 * - `useConfigStore` from `src/store/configStore.ts`
 * - `useDownloadPageStore` from `src/store/downloadPageStore.ts`
 */
export {};

